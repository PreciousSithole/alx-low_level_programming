#include "main.h"

/**
 * _isupper - check if a is upper
 * @c: The number to be checked
 *
 * Return: 1 for upper or 0 for any else
 */

int _isupper(int x)
{
	if (x >= 65 && c <= 90)
	{	
		return (1);
	}
		return (0);
}


